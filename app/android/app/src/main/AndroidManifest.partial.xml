<!--
  Append to the generated AndroidManifest.xml inside the <application> tag.
  Adds: Universal-Links handler for astappplm:// scheme + assetlinks-backed
  https://app.astahealthtech.com host, foreground service for alerts, FCM service.
-->

<service
  android:name=".modules.AlertForegroundService"
  android:exported="false"
  android:foregroundServiceType="specialUse" />

<!--
<service
  android:name=".modules.AlertFcmService"
  android:exported="false">
  <intent-filter>
    <action android:name="com.google.firebase.MESSAGING_EVENT" />
  </intent-filter>
</service>
-->

<activity
  android:name=".MainActivity"
  android:launchMode="singleTask">
  <intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="https" android:host="app.astahealthtech.com" />
    <data android:scheme="astappplm" />
  </intent-filter>
</activity>

<!-- Permissions to add at the manifest root: -->
<!--
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.USE_BIOMETRIC" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
<uses-permission android:name="android.permission.VIBRATE" />
<uses-permission android:name="android.permission.INTERNET" />
-->
